# Copy this file to .env. Bun loads .env automatically.

# Required when running with --api.
MEDIA_TAG_API_KEY=sk-your-api-key-here

# Optional endpoint/model overrides are CLI flags:
# bun run src/cli.ts tag ./library --api --api-base-url https://api.openai.com/v1 --api-model gpt-4o-mini
