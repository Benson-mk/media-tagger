{
  "name": "media-tagger",
  "type": "module",
  "private": true,
  "module": "src/cli.ts",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --target bun --outdir dist",
    "check": "biome check src package.json tsconfig.json biome.json && tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "typescript": "^6.0.3",
    "bun-types": "^1.3.14"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "exifr": "^7.1.3",
    "image-size": "^2.0.2",
    "zod": "^4.4.3"
  }
}
